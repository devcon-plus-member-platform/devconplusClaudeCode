export default function NewBadge() {
  return (
    <span className="shrink-0 whitespace-nowrap text-[10px] font-semibold uppercase tracking-[0.5px] text-primary bg-primary/10 px-2 py-0.5 rounded-full">
      New
    </span>
  )
}
